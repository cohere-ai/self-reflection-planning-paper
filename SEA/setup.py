from setuptools import setup

setup(
   name='deep_planner',
   version='0.1',
   description='A package for self-reflective planning with LLMs.',
   author='Tomas Goldsack',
   author_email='tomasgoldsack@cohere.com',
   packages=['deep_planner'],  #same as name
   install_requires=[], #external packages as dependencies
)